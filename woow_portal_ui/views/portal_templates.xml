<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!-- Load Google Fonts (Outfit + Noto Sans TC)                    -->
    <!-- ============================================================ -->
    <template id="portal_google_fonts" name="Woow Google Fonts"
              inherit_id="portal.frontend_layout" priority="10">
        <xpath expr="//head/meta[1]" position="before">
            <link rel="preconnect" href="https://fonts.googleapis.com"/>
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous"/>
            <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&amp;family=Noto+Sans+TC:wght@400;500;700;900&amp;display=swap" rel="stylesheet"/>
        </xpath>
    </template>

    <!-- ============================================================ -->
    <!-- Language Switcher in User Dropdown                           -->
    <!-- ============================================================ -->
    <template id="portal_user_lang_menu" name="Woow Language in User Menu"
              inherit_id="portal.user_dropdown" priority="20">
        <xpath expr="//div[@id='o_logout_divider']" position="before">
            <t t-set="installed_langs" t-value="request.env['res.lang'].get_installed()"/>
            <t t-if="len(installed_langs) > 1">
                <div class="dropdown-divider"/>
                <div class="dropdown-header small text-muted px-3 py-1">
                    <i class="fa fa-globe me-1"/>Language
                </div>
                <t t-foreach="installed_langs" t-as="ilang">
                    <a t-attf-class="dropdown-item ps-3 wt-lang-item #{'active' if ilang[0] == (request.env.user.lang or 'en_US') else ''}"
                       t-attf-href="/my/set_lang/#{ilang[0]}"
                       role="menuitem">
                        <t t-esc="ilang[1]"/>
                    </a>
                </t>
            </t>
        </xpath>
    </template>

    <!-- ============================================================ -->
    <!-- Replace "My account" header with greeting card               -->
    <!-- ============================================================ -->
    <template id="portal_greeting_card" name="Woow Portal Greeting Card"
              inherit_id="portal.portal_layout" priority="10">
        <!-- Remove the default "My account" header — greeting card is inside portal_my_home instead -->
        <xpath expr="//div[hasclass('wrapper')][.//h3[hasclass('my-3')]]" position="replace">
            <!-- Empty: greeting card rendered inside portal_my_home grid for correct column placement -->
        </xpath>
    </template>

    <!-- ============================================================ -->
    <!-- Portal home: search bar + notification preview               -->
    <!-- ============================================================ -->
    <template id="portal_my_home_enhanced" name="Woow Enhanced Portal Home"
              inherit_id="portal.portal_my_home" priority="10">

        <!-- Greeting card + Search bar + Notification preview — before the module grid -->
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">

            <!-- Greeting Card (inside grid for column 1 placement) -->
            <div class="wt-greeting-card" t-if="greeting">
                <div class="wt-greet-row">
                    <div class="wt-greet-avatar">
                        <t t-set="partner_name" t-value="user_id.partner_id.name or ''"/>
                        <t t-esc="partner_name[:1].upper() if partner_name else 'U'"/>
                    </div>
                    <div class="wt-greet-body">
                        <h1 class="wt-greet-h1">
                            <t t-esc="greeting"/>,<br class="wt-greet-br"/>
                            <t t-esc="greeting_name or user_id.partner_id.name"/>
                        </h1>
                        <div class="wt-greet-meta">
                            <span class="wt-gm-chip" t-if="today_date_line1">
                                <i class="fa fa-calendar-o"/>
                                <t t-esc="today_date_line1"/>
                            </span>
                            <span class="wt-gm-chip wt-gm-time" t-if="today_date_line2">
                                <i class="fa fa-clock-o"/>
                                <t t-esc="today_date_line2"/>
                            </span>
                        </div>
                    </div>
                    <a href="/my/account" class="wt-greet-edit" title="Edit profile">
                        <i class="fa fa-pencil"/>
                    </a>
                </div>
            </div>

            <!-- Search Bar (WoowTech design) -->
            <div class="wt-hero-search">
                <i class="fa fa-search" style="color: var(--wt-ink-3);"/>
                <input type="text" id="wpu_module_search"
                       placeholder="Search modules or notifications..."/>
            </div>

            <!-- Notification Preview Block (WoowTech design) -->
            <t t-set="has_any" t-value="message_previews or sys_notif_previews or activity_previews"/>
            <div class="wt-notif-block" t-if="has_any">
                <div class="wt-notif-block-head">
                    <h3 class="wt-nb-title">
                        Recent Notifications
                        <t t-if="total_unread_count">
                            <span class="wt-nb-count"><t t-esc="total_unread_count"/></span>
                        </t>
                    </h3>
                    <a href="/my/notifications" class="wt-nb-viewall">
                        View All <i class="fa fa-angle-right ms-1"/>
                    </a>
                </div>

                <!-- Messages group -->
                <t t-if="unread_message_count or message_previews">
                    <div class="wt-notif-group">
                        <div class="wt-ng-head">
                            <span class="wt-ng-icon wt-ng-msg"><i class="fa fa-comment" style="font-size:11px;"/></span>
                            <span class="wt-ng-label">Messages</span>
                            <t t-if="unread_message_count"><span class="wt-ng-num"><t t-esc="unread_message_count"/></span></t>
                        </div>
                        <t t-foreach="message_previews" t-as="notif">
                            <a href="/my/notifications?tab=message" class="wt-notif-item">
                                <div class="wt-ni-avatar wt-ni-blue"><i class="fa fa-comment" style="font-size:13px;"/></div>
                                <div class="wt-ni-body">
                                    <div class="wt-ni-t"><t t-esc="notif['subject']"/></div>
                                    <div class="wt-ni-s"><t t-esc="notif['record_name']"/></div>
                                </div>
                                <span class="wt-ni-chev"><i class="fa fa-angle-right"/></span>
                            </a>
                        </t>
                    </div>
                </t>

                <!-- Notifications group -->
                <t t-if="unread_sys_notif_count or sys_notif_previews">
                    <div class="wt-notif-group">
                        <div class="wt-ng-head">
                            <span class="wt-ng-icon wt-ng-bell"><i class="fa fa-bell" style="font-size:11px;"/></span>
                            <span class="wt-ng-label">Notifications</span>
                            <t t-if="unread_sys_notif_count"><span class="wt-ng-num wt-ng-bell-num"><t t-esc="unread_sys_notif_count"/></span></t>
                        </div>
                        <t t-foreach="sys_notif_previews" t-as="notif">
                            <a href="/my/notifications?tab=notification" class="wt-notif-item">
                                <div class="wt-ni-avatar wt-ni-blue"><i class="fa fa-bell" style="font-size:13px;"/></div>
                                <div class="wt-ni-body">
                                    <div class="wt-ni-t"><t t-esc="notif['subject']"/></div>
                                    <div class="wt-ni-s"><t t-esc="notif['record_name']"/></div>
                                </div>
                                <span class="wt-ni-chev"><i class="fa fa-angle-right"/></span>
                            </a>
                        </t>
                    </div>
                </t>

                <!-- Activities group (internal users only) -->
                <t t-if="is_internal_user and (activity_count or activity_previews)">
                    <div class="wt-notif-group">
                        <div class="wt-ng-head">
                            <span class="wt-ng-icon wt-ng-activity"><i class="fa fa-clock-o" style="font-size:11px;"/></span>
                            <span class="wt-ng-label">To-Do</span>
                            <t t-if="activity_count"><span class="wt-ng-num wt-ng-activity-num"><t t-esc="activity_count"/></span></t>
                        </div>
                        <t t-foreach="activity_previews" t-as="act">
                            <a href="/my/notifications?tab=activity" class="wt-notif-item">
                                <div class="wt-ni-avatar wt-ni-blue"><i class="fa fa-clock-o" style="font-size:13px;"/></div>
                                <div class="wt-ni-body">
                                    <div class="wt-ni-t"><t t-esc="act['summary']"/></div>
                                    <div class="wt-ni-s"><t t-esc="act['res_name']"/></div>
                                </div>
                                <span class="wt-ni-chev"><i class="fa fa-angle-right"/></span>
                            </a>
                        </t>
                    </div>
                </t>
            </div>

            <!-- Dynamic alert banners: rendered via Odoo's portal_alert_category -->
            <!-- Styling handled by CSS (#portal_alert_category rules in woowtech_theme.css) -->

        </xpath>

        <!-- Add ID to module grid for search filtering -->
        <xpath expr="//div[hasclass('o_portal_docs')]" position="attributes">
            <attribute name="id">wpu_module_grid</attribute>
        </xpath>

        <!-- Profile sidebar — after the module grid (sibling, not wrapper) -->
        <xpath expr="//div[hasclass('o_portal_docs')]" position="after">
            <div class="wt-profile-aside">
                <div class="wt-pa-head">
                    <div class="wt-pa-avatar">
                        <t t-set="pa_name" t-value="user_id.partner_id.name or ''"/>
                        <t t-esc="pa_name[:1].upper() if pa_name else 'U'"/>
                    </div>
                    <div class="wt-pa-mail"><t t-esc="user_id.partner_id.email or user_id.login"/></div>
                </div>
                <div class="wt-pa-rows">
                    <t t-if="user_id.partner_id.contact_address_inline">
                        <div class="wt-pa-row">
                            <span class="wt-pi-icon"><i class="fa fa-map-marker"/></span>
                            <span class="wt-pi-text"><t t-esc="user_id.partner_id.contact_address_inline"/></span>
                        </div>
                    </t>
                    <t t-if="user_id.partner_id.phone or user_id.partner_id.mobile">
                        <div class="wt-pa-row">
                            <span class="wt-pi-icon"><i class="fa fa-phone"/></span>
                            <span class="wt-pi-text"><t t-esc="user_id.partner_id.phone or user_id.partner_id.mobile"/></span>
                        </div>
                    </t>
                    <t t-if="user_id.partner_id.email">
                        <div class="wt-pa-row">
                            <span class="wt-pi-icon"><i class="fa fa-envelope-o"/></span>
                            <span class="wt-pi-text"><t t-esc="user_id.partner_id.email"/></span>
                        </div>
                    </t>
                </div>
                <a href="/my/account" class="wt-pa-edit">
                    <i class="fa fa-pencil"/> Edit information
                </a>
            </div>
        </xpath>

    </template>

    <!-- ============================================================ -->
    <!-- Notification List Page — /my/notifications                   -->
    <!-- ============================================================ -->
    <template id="portal_notifications_page" name="Portal Notifications Page">
        <t t-call="portal.portal_layout">
            <t t-set="no_breadcrumbs" t-value="True"/>

            <div class="container py-3">
              <t t-call="portal.portal_breadcrumbs"/>
              <div class="nc-page">

                <!-- ===== Header Row ===== -->
                <div class="nc-head-row">
                    <div>
                        <h1 class="nc-h1">Notification Center</h1>
                    </div>
                    <div class="nc-actions">
                        <!-- Mobile filter toggle (hidden on desktop via CSS) -->
                        <button type="button"
                                class="nc-filter-toggle"
                                id="wpu_notif_searchbar_toggle">
                            <i class="fa fa-filter"/>
                            <span class="nc-ft-label">Filters</span>
                            <i class="fa fa-chevron-down nc-ft-chev"/>
                        </button>
                        <t t-if="unread_notif_count">
                            <button type="button" class="nc-mark-all"
                                    id="wpu_mark_all_read">
                                <i class="fa fa-check me-1"/>Mark all read
                            </button>
                        </t>
                    </div>
                </div>

                <!-- ===== 2-Column Layout ===== -->
                <div class="nc-layout">

                    <!-- LEFT SIDEBAR -->
                    <aside class="nc-side" id="wpu_notif_sidebar">

                        <!-- Vertical Tab Nav -->
                        <nav class="nc-tabs wpu-notif-tabs">
                            <a t-attf-href="/my/notifications?tab=all"
                               t-attf-class="nc-tab wpu-notif-tab tone-ink #{'active' if current_tab == 'all' else ''}">
                                <span class="nc-tab-ico"><i class="fa fa-fw fa-asterisk"/></span>
                                <span class="nc-tab-l">All</span>
                                <span class="nc-tab-c" data-unread-badge="all">
                                    <t t-esc="unread_all_count or 0"/>
                                </span>
                            </a>
                            <a t-attf-href="/my/notifications?tab=message"
                               t-attf-class="nc-tab wpu-notif-tab tone-coral #{'active' if current_tab == 'message' else ''}">
                                <span class="nc-tab-ico"><i class="fa fa-fw fa-comment"/></span>
                                <span class="nc-tab-l">Messages</span>
                                <span class="nc-tab-c" data-unread-badge="message">
                                    <t t-esc="unread_message_count or 0"/>
                                </span>
                            </a>
                            <a t-attf-href="/my/notifications?tab=notification"
                               t-attf-class="nc-tab wpu-notif-tab tone-blue #{'active' if current_tab == 'notification' else ''}">
                                <span class="nc-tab-ico"><i class="fa fa-fw fa-bell"/></span>
                                <span class="nc-tab-l">Notifications</span>
                                <span class="nc-tab-c" data-unread-badge="notification">
                                    <t t-esc="unread_sys_notif_count or 0"/>
                                </span>
                            </a>
                            <t t-if="is_internal_user">
                                <a t-attf-href="/my/notifications?tab=activity"
                                   t-attf-class="nc-tab wpu-notif-tab tone-green #{'active' if current_tab == 'activity' else ''}">
                                    <span class="nc-tab-ico"><i class="fa fa-fw fa-check-square-o"/></span>
                                    <span class="nc-tab-l">To-Do</span>
                                    <span class="nc-tab-c" data-activity-badge="activity">
                                        <t t-esc="activity_count or 0"/>
                                    </span>
                                </a>
                            </t>
                        </nav>

                        <!-- Filter / Sort / Group / Search Card -->
                        <section class="nc-filter-card wpu-notif-searchbar" id="wpu_notif_searchbar">
                            <!-- Search -->
                            <div class="ncf-search">
                                <i class="fa fa-search" style="color: var(--wt-ink-3);"/>
                                <input type="text" id="wpu_notif_search_input"
                                       placeholder="Search notifications..."/>
                                <button type="button" class="ncf-search-clear d-none"
                                        id="wpu_notif_search_btn" aria-label="Clear">
                                    <i class="fa fa-times"/>
                                </button>
                            </div>
                            <!-- Sort -->
                            <div class="ncf-segs">
                                <div class="ncf-row">
                                    <span class="ncf-label">Sort</span>
                                    <div class="ncf-seg">
                                        <button class="ncf-seg-btn wpu-notif-sort-btn active"
                                                data-sort="newest">Newest</button>
                                        <button class="ncf-seg-btn wpu-notif-sort-btn"
                                                data-sort="oldest">Oldest</button>
                                    </div>
                                </div>
                                <!-- Filter -->
                                <div class="ncf-row">
                                    <span class="ncf-label">Filter</span>
                                    <div class="ncf-seg">
                                        <button class="ncf-seg-btn wpu-notif-filter-btn active"
                                                data-filter="all">All</button>
                                        <button class="ncf-seg-btn wpu-notif-filter-btn"
                                                data-filter="unread">Unread</button>
                                        <button class="ncf-seg-btn wpu-notif-filter-btn"
                                                data-filter="read">Read</button>
                                    </div>
                                </div>
                                <!-- Group -->
                                <div class="ncf-row">
                                    <span class="ncf-label">Group</span>
                                    <div class="ncf-seg">
                                        <button class="ncf-seg-btn wpu-notif-group-btn active"
                                                data-group="none">None</button>
                                        <button class="ncf-seg-btn wpu-notif-group-btn"
                                                data-group="source">Source</button>
                                    </div>
                                </div>
                            </div>
                        </section>

                    </aside>

                    <!-- RIGHT MAIN CONTENT -->
                    <div class="nc-main">

                        <!-- Notification List -->
                        <div class="wpu-notif-list" id="wpu_notif_list">
                            <t t-if="notification_items">
                                <t t-foreach="notification_items" t-as="item">
                                    <!-- Notification-type item -->
                                    <t t-if="item.get('notif_id')">
                                        <div t-attf-class="wpu-notif-card-wrapper #{'wpu-notif-read' if item['is_read'] else 'wpu-notif-unread'}"
                                             t-att-data-notif-id="item['notif_id']"
                                             data-item-type="notification">
                                            <div class="wpu-notif-swipe-bg">
                                                <t t-if="item['is_read']">
                                                    <i class="fa fa-times"/>
                                                    <span>Dismiss</span>
                                                </t>
                                                <t t-else="">
                                                    <i class="fa fa-check"/>
                                                    <span>Read</span>
                                                </t>
                                            </div>
                                            <div class="wpu-notif-card" data-action="show-detail">
                                                <div class="wpu-notif-card-icon">
                                                    <i t-attf-class="fa #{item.get('icon', 'fa-bell')}"/>
                                                </div>
                                                <div class="wpu-notif-card-body">
                                                    <div class="wpu-notif-card-title">
                                                        <t t-esc="item['subject']"/>
                                                    </div>
                                                    <div class="wpu-notif-card-meta">
                                                        <span class="wpu-notif-card-source">
                                                            <t t-esc="item['record_name']"/>
                                                        </span>
                                                        <span class="nc-meta-sep">·</span>
                                                        <span class="wpu-notif-card-date">
                                                            <t t-esc="item['time_ago']"/>
                                                        </span>
                                                    </div>
                                                    <t t-if="item.get('tracking_summary')">
                                                        <div class="wpu-notif-card-preview">
                                                            <t t-esc="item['tracking_summary']"/>
                                                        </div>
                                                    </t>
                                                    <t t-if="item.get('body_preview') and not item.get('tracking_summary')">
                                                        <div class="wpu-notif-card-preview">
                                                            <t t-esc="item['body_preview']"/>
                                                        </div>
                                                    </t>
                                                </div>
                                                <div class="wpu-notif-card-hint">
                                                    <span t-attf-class="wpu-unread-dot #{'d-none' if item['is_read'] else ''}"
                                                          t-att-data-notif-id="item['notif_id']"/>
                                                    <i class="fa fa-angle-right nc-chev"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                    <!-- Activity-type item -->
                                    <t t-if="item.get('activity_id')">
                                        <div t-attf-class="wpu-notif-card-wrapper wpu-activity-#{item.get('state', 'planned')}"
                                             t-att-data-activity-id="item['activity_id']"
                                             data-item-type="activity">
                                            <div class="wpu-notif-swipe-bg">
                                                <i class="fa fa-check"/>
                                                <span>Done</span>
                                            </div>
                                            <div class="wpu-notif-card" data-action="show-detail">
                                                <div class="wpu-notif-card-icon">
                                                    <i t-attf-class="fa #{item.get('icon', 'fa-clock-o')}"/>
                                                </div>
                                                <div class="wpu-notif-card-body">
                                                    <div class="wpu-notif-card-title">
                                                        <t t-esc="item['summary']"/>
                                                    </div>
                                                    <div class="wpu-notif-card-meta">
                                                        <span class="wpu-notif-card-source">
                                                            <t t-esc="item['res_name']"/>
                                                        </span>
                                                        <t t-if="item.get('date_deadline')">
                                                            <span class="nc-meta-sep">·</span>
                                                            <span class="wpu-notif-card-date">
                                                                <i class="fa fa-calendar-o me-1"/>
                                                                <t t-esc="item['date_deadline']"/>
                                                            </span>
                                                        </t>
                                                    </div>
                                                    <div class="wpu-notif-card-type">
                                                        <span class="nc-tag-todo">
                                                            <t t-esc="item.get('activity_type', 'Activity')"/>
                                                        </span>
                                                    </div>
                                                </div>
                                                <div class="wpu-notif-card-hint">
                                                    <span class="wpu-activity-dot"
                                                          t-att-data-activity-id="item['activity_id']"/>
                                                    <i class="fa fa-angle-right nc-chev"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </t>
                            </t>
                            <t t-else="">
                                <div class="nc-empty">
                                    <span class="nc-empty-ico"><i class="fa fa-bell fa-lg"/></span>
                                    <div class="nc-empty-t">No notifications</div>
                                    <div class="nc-empty-s">You're all caught up.</div>
                                </div>
                            </t>
                        </div>

                        <!-- Swipe hint -->
                        <t t-if="notification_items">
                            <div class="wpu-swipe-hint text-center text-muted mt-2 mb-4"
                                 id="wpu_swipe_hint">
                                <i class="fa fa-long-arrow-right me-1"/>
                                <span>Swipe right to mark as read / done</span>
                            </div>
                        </t>

                    </div><!-- /nc-main -->

                </div><!-- /nc-layout -->

              </div><!-- /nc-page -->
            </div>

            <!-- Detail Modal -->
            <div class="wpu-notif-modal-overlay" id="wpu_notif_modal_overlay"
                 style="display: none;">
                <div class="wpu-notif-modal" id="wpu_notif_modal">
                    <div class="wpu-notif-modal-header">
                        <h5 class="wpu-notif-modal-title" id="wpu_modal_title"/>
                        <button type="button" class="btn-close"
                                id="wpu_modal_close"/>
                    </div>
                    <div class="wpu-notif-modal-body" id="wpu_modal_body"/>
                    <div class="wpu-notif-modal-footer" id="wpu_modal_footer">
                        <a href="#" class="btn btn-sm btn-outline-primary me-2"
                           id="wpu_modal_doc_link" target="_blank"
                           style="display: none;">
                            <i class="fa fa-external-link me-1"/><span>View Document</span>
                        </a>
                        <button type="button"
                                class="btn btn-sm btn-primary"
                                id="wpu_modal_action_btn"
                                style="display: none;">
                            <i class="fa fa-check me-1"/><span>Mark as read</span>
                        </button>
                    </div>
                </div>
            </div>

        </t>
    </template>


    <!-- Remove "Powered by Odoo" from portal record sidebar -->
    <template id="portal_record_sidebar_hide_powered" inherit_id="portal.portal_record_sidebar">
        <xpath expr="//div[hasclass('o_portal_sidebar_content')]/div[hasclass('text-muted')][last()]" position="replace"/>
    </template>

    <!-- ============================================================ -->
    <!-- Orders Page — Card grid (replaces table)                     -->
    <!-- ============================================================ -->
    <record id="portal_orders_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Orders Card View</field>
        <field name="inherit_id" ref="sale.portal_my_orders"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-orders-grid" t-if="orders">
                    <t t-foreach="orders" t-as="order">
                        <a t-att-href="order.get_portal_url()" class="wt-order-card">
                            <div class="wt-oc-head">
                                <div class="wt-oc-icon">
                                    <i class="fa fa-shopping-bag" style="font-size: 14px;"/>
                                </div>
                                <span class="wt-oc-num"><t t-esc="order.name"/></span>
                            </div>
                            <div class="wt-oc-meta">
                                <div class="wt-oc-row">
                                    <span class="wt-oc-label">Order Date</span>
                                    <span class="wt-oc-value"><t t-esc="order.date_order" t-options='{"widget": "datetime"}'/></span>
                                </div>
                                <div class="wt-oc-row">
                                    <span class="wt-oc-label">Total</span>
                                    <span class="wt-oc-value wt-oc-total">
                                        <t t-esc="order.amount_total" t-options='{"widget": "monetary", "display_currency": order.currency_id}'/>
                                    </span>
                                </div>
                            </div>
                            <div class="wt-oc-foot">
                                <span>View order</span>
                                <i class="fa fa-angle-right"/>
                            </div>
                        </a>
                    </t>
                </div>
                <t t-if="not orders">
                    <p class="alert alert-warning">There are currently no sales orders for your account.</p>
                </t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3">
                    <t t-call="portal.pager"/>
                </div>
            </xpath>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Invoices Page — Card grid (replaces table)                   -->
    <!-- ============================================================ -->
    <record id="portal_invoices_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Invoices Card View</field>
        <field name="inherit_id" ref="account.portal_my_invoices"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
            <div class="wt-invoices-grid" t-if="invoices">
                <t t-foreach="invoices" t-as="invoice_data">
                    <t t-set="invoice" t-value="invoice_data['invoice']"/>
                    <t t-set="is_overdue" t-value="invoice.payment_state in ('not_paid', 'partial') and invoice.invoice_date_due and invoice.invoice_date_due &lt; datetime.date.today()"/>
                    <a t-att-href="invoice.get_portal_url()"
                       t-attf-class="wt-invoice-card #{'wt-is-overdue' if is_overdue else ''}">
                        <div class="wt-ic-head">
                            <div class="wt-ic-icon">
                                <i class="fa fa-file-text-o" style="font-size: 14px;"/>
                            </div>
                            <span class="wt-ic-num">
                                <t t-if="invoice.name != '/'" t-esc="invoice.name"/>
                                <em t-else="">Draft</em>
                            </span>
                            <t t-if="invoice.state == 'posted' and invoice.currency_id.is_zero(invoice.amount_residual)">
                                <span class="wt-badge wt-badge-paid">Paid</span>
                            </t>
                            <t t-elif="invoice.payment_state == 'reversed'">
                                <span class="wt-badge wt-badge-reversed">Reversed</span>
                            </t>
                            <t t-elif="invoice.state == 'cancel'">
                                <span class="wt-badge wt-badge-cancelled">Cancelled</span>
                            </t>
                            <t t-elif="invoice.payment_state == 'in_payment'">
                                <span class="wt-badge wt-badge-processing">Processing</span>
                            </t>
                            <t t-elif="invoice.state == 'posted'">
                                <span class="wt-badge wt-badge-waiting">Waiting</span>
                            </t>
                        </div>
                        <div class="wt-ic-meta">
                            <div class="wt-ic-row">
                                <span class="wt-ic-label">Invoice Date</span>
                                <span class="wt-ic-value"><t t-esc="invoice.invoice_date" t-options='{"widget": "date"}'/></span>
                            </div>
                            <div class="wt-ic-row">
                                <span class="wt-ic-label">Due Date</span>
                                <span class="wt-ic-value"><t t-esc="invoice.invoice_date_due" t-options='{"widget": "date"}'/></span>
                            </div>
                            <div class="wt-ic-row">
                                <span class="wt-ic-label">Amount Due</span>
                                <span class="wt-ic-value wt-ic-amount">
                                    <t t-esc="invoice.amount_residual" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/>
                                </span>
                            </div>
                        </div>
                        <div class="wt-ic-foot" t-if="invoice.state == 'posted' and not invoice.currency_id.is_zero(invoice.amount_residual)">
                            <t t-if="is_overdue">
                                <span class="wt-inv-due-note">Overdue</span>
                            </t>
                            <span class="wt-inv-paynow">
                                <i class="fa fa-credit-card" style="font-size: 10px;"/> Pay Now
                            </span>
                        </div>
                    </a>
                </t>
            </div>
            <t t-if="not invoices">
                <p class="alert alert-warning">There are currently no invoices and payments for your account.</p>
            </t>
            <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3">
                <t t-call="portal.pager"/>
            </div>
            </xpath>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Security Page — Enable breadcrumb + set page_name           -->
    <!-- ============================================================ -->
    <template id="portal_security_breadcrumb" name="Woow Security Breadcrumb"
              inherit_id="portal.portal_my_security" priority="10">
        <xpath expr="//t[@t-set='no_breadcrumbs']" position="replace">
            <t t-set="page_name" t-value="'security'"/>
        </xpath>
    </template>

    <!-- Security Page — Add breadcrumb item -->
    <template id="portal_breadcrumbs_security" name="Security Breadcrumb Item"
              inherit_id="portal.portal_breadcrumbs" priority="30">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'security'" class="breadcrumb-item active">
                Connection &amp; Security
            </li>
        </xpath>
    </template>

    <!-- Notifications Page — Add breadcrumb item -->
    <template id="portal_breadcrumbs_notifications" name="Notifications Breadcrumb Item"
              inherit_id="portal.portal_breadcrumbs" priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'notifications'" class="breadcrumb-item active">
                Notification Center
            </li>
        </xpath>
    </template>


    <!-- ============================================================ -->
    <!-- Card views for additional portal pages (forcecreate=False    -->
    <!-- for safe cross-module inheritance — silently skips if the    -->
    <!-- parent module is not installed)                              -->
    <!-- ============================================================ -->

    <!-- Quotations Card View -->
    <record id="portal_quotations_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Quotations Card View</field>
        <field name="inherit_id" ref="sale.portal_my_quotations"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid" t-if="quotations">
                    <t t-foreach="quotations" t-as="quotation">
                        <a t-att-href="quotation.get_portal_url()" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-file-text-o" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="quotation.name"/></span>
                                <t t-if="quotation.state == 'cancel'"><span class="wt-badge wt-badge-cancelled">Cancelled</span></t>
                                <t t-elif="quotation.is_expired"><span class="wt-badge wt-badge-cancelled">Expired</span></t>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Date</span>
                                    <span class="wt-pc-value"><t t-esc="quotation.date_order" t-options='{"widget": "date"}'/></span>
                                </div>
                                <div class="wt-pc-row" t-if="quotation.validity_date">
                                    <span class="wt-pc-label">Valid Until</span>
                                    <span class="wt-pc-value"><t t-esc="quotation.validity_date" t-options='{"widget": "date"}'/></span>
                                </div>
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Total</span>
                                    <span class="wt-pc-value wt-pc-total"><t t-esc="quotation.amount_total" t-options='{"widget": "monetary", "display_currency": quotation.currency_id}'/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View quotation</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not quotations"><p class="alert alert-warning">There are currently no quotations for your account.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Projects Card View -->
    <record id="portal_projects_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Projects Card View</field>
        <field name="inherit_id" ref="project.portal_my_projects"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid" t-if="projects">
                    <t t-foreach="projects" t-as="project">
                        <a t-attf-href="/my/projects/#{project.id}" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-folder-o" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="project.name"/></span>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Tasks</span>
                                    <span class="wt-pc-value"><t t-esc="project.task_count"/> <t t-esc="project.label_tasks"/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View project</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not projects"><p class="alert alert-warning">There are currently no projects for your account.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Tasks Card View -->
    <record id="portal_tasks_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Tasks Card View</field>
        <field name="inherit_id" ref="project.portal_tasks_list"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <t t-set="task_fields" t-value="request.env['project.task'].fields_get()"/>
                <div class="wt-card-grid">
                    <t t-foreach="grouped_tasks" t-as="tasks">
                        <t t-foreach="tasks" t-as="task">
                            <a t-attf-href="/my/#{task_url}/#{task.id}?{{ keep_query() }}" class="wt-portal-card">
                                <div class="wt-pc-head">
                                    <div class="wt-pc-icon"><i class="fa fa-check-square-o" style="font-size:14px;"/></div>
                                    <span class="wt-pc-num">#<t t-esc="task.id"/> <t t-esc="task.name"/></span>
                                </div>
                                <div class="wt-pc-meta">
                                    <div class="wt-pc-row wt-field-id">
                                        <span class="wt-pc-label">ID</span>
                                        <span class="wt-pc-value">#<t t-esc="task.id"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-stage_id">
                                        <span class="wt-pc-label">Stage</span>
                                        <span class="wt-pc-value"><t t-esc="task.stage_id.name"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-date_deadline" t-if="task.date_deadline">
                                        <span class="wt-pc-label">Deadline</span>
                                        <span class="wt-pc-value" t-field="task.date_deadline" t-options='{"widget": "date"}'/>
                                    </div>
                                    <div class="wt-pc-row wt-field-tag_ids" t-if="task.tag_ids">
                                        <span class="wt-pc-label">Tags</span>
                                        <span class="wt-pc-value"><t t-foreach="task.tag_ids" t-as="tag"><span class="badge bg-info me-1"><t t-esc="tag.name"/></span></t></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-allocated_hours" t-if="task.allocated_hours">
                                        <span class="wt-pc-label">Allocated Time</span>
                                        <span class="wt-pc-value"><t t-esc="'%02d:%02d' % (int(task.allocated_hours), int(task.allocated_hours % 1 * 60))"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-effective_hours" t-if="'effective_hours' in task_fields">
                                        <span class="wt-pc-label">Time Spent</span>
                                        <span class="wt-pc-value"><t t-esc="'%02d:%02d' % (int(task.effective_hours), int(task.effective_hours % 1 * 60))"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-subtask_effective_hours" t-if="'subtask_effective_hours' in task_fields and task.subtask_effective_hours">
                                        <span class="wt-pc-label">Sub-task Time</span>
                                        <span class="wt-pc-value"><t t-esc="'%02d:%02d' % (int(task.subtask_effective_hours), int(task.subtask_effective_hours % 1 * 60))"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-total_hours_spent" t-if="'total_hours_spent' in task_fields and task.total_hours_spent">
                                        <span class="wt-pc-label">Total Time Spent</span>
                                        <span class="wt-pc-value"><t t-esc="'%02d:%02d' % (int(task.total_hours_spent), int(task.total_hours_spent % 1 * 60))"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-remaining_hours" t-if="'remaining_hours' in task_fields and task.allocated_hours">
                                        <span class="wt-pc-label">Time Remaining</span>
                                        <span class="wt-pc-value"><t t-esc="'%02d:%02d' % (int(task.remaining_hours), int(task.remaining_hours % 1 * 60))"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-remaining_hours_so" t-if="'remaining_hours_so' in task_fields and task.remaining_hours_so">
                                        <span class="wt-pc-label">Remaining on SO</span>
                                        <span class="wt-pc-value"><t t-esc="'%02d:%02d' % (int(task.remaining_hours_so), int(task.remaining_hours_so % 1 * 60))"/></span>
                                    </div>
                                    <div class="wt-pc-row wt-field-progress" t-if="'progress' in task_fields and task.allocated_hours">
                                        <span class="wt-pc-label">Progress</span>
                                        <span class="wt-pc-value"><t t-esc="'%.0f' % (task.progress)"/>%</span>
                                    </div>
                                    <div class="wt-pc-row wt-field-date_last_stage_update" t-if="task.date_last_stage_update">
                                        <span class="wt-pc-label">Last Stage Update</span>
                                        <span class="wt-pc-value" t-field="task.date_last_stage_update" t-options='{"widget": "date"}'/>
                                    </div>
                                    <div class="wt-pc-row wt-field-project_id" t-if="task.project_id">
                                        <span class="wt-pc-label">Project</span>
                                        <span class="wt-pc-value"><t t-esc="task.project_id.name"/></span>
                                    </div>
                                </div>
                                <div class="wt-pc-foot"><span>View task</span><i class="fa fa-angle-right"/></div>
                            </a>
                        </t>
                    </t>
                </div>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Project Editor Action Bar + Modals (for full-edit collaborators) -->
    <record id="portal_project_editor_bar" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Project Editor Bar</field>
        <field name="inherit_id" ref="project.portal_my_project"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_searchbar']" position="before">
                <div t-if="is_editor" class="wt-editor-bar d-flex gap-2" id="wtEditorBar">
                    <button class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#addTaskModal">
                        <i class="fa fa-plus me-1"/>Add Task
                    </button>
                    <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal" data-bs-target="#addStageModal">
                        <i class="fa fa-columns me-1"/>Add Stage
                    </button>
                    <div class="dropdown d-inline-block ms-auto">
                        <button class="btn btn-outline-secondary btn-sm" data-bs-toggle="dropdown" data-bs-auto-close="outside" title="Configure card fields">
                            <i class="fa fa-cog"/>
                        </button>
                        <div class="dropdown-menu dropdown-menu-end p-3" id="fieldChooser" style="min-width: 220px;">
                            <div class="fw-bold mb-2 small text-muted">Card Fields</div>
                            <t t-set="fc_fields" t-value="request.env['project.task'].fields_get()"/>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="id" checked="checked" id="fc_id"/><label class="form-check-label" for="fc_id">ID</label></div>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="stage_id" checked="checked" id="fc_stage_id"/><label class="form-check-label" for="fc_stage_id">Stage</label></div>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="date_deadline" checked="checked" id="fc_date_deadline"/><label class="form-check-label" for="fc_date_deadline">Deadline</label></div>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="tag_ids" checked="checked" id="fc_tag_ids"/><label class="form-check-label" for="fc_tag_ids">Tags</label></div>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="allocated_hours" id="fc_allocated_hours"/><label class="form-check-label" for="fc_allocated_hours">Allocated Time</label></div>
                            <div class="form-check" t-if="'effective_hours' in fc_fields"><input class="form-check-input" type="checkbox" data-field="effective_hours" id="fc_effective_hours"/><label class="form-check-label" for="fc_effective_hours">Time Spent</label></div>
                            <div class="form-check" t-if="'subtask_effective_hours' in fc_fields"><input class="form-check-input" type="checkbox" data-field="subtask_effective_hours" id="fc_subtask_effective_hours"/><label class="form-check-label" for="fc_subtask_effective_hours">Sub-task Time</label></div>
                            <div class="form-check" t-if="'total_hours_spent' in fc_fields"><input class="form-check-input" type="checkbox" data-field="total_hours_spent" id="fc_total_hours_spent"/><label class="form-check-label" for="fc_total_hours_spent">Total Time Spent</label></div>
                            <div class="form-check" t-if="'remaining_hours' in fc_fields"><input class="form-check-input" type="checkbox" data-field="remaining_hours" id="fc_remaining_hours"/><label class="form-check-label" for="fc_remaining_hours">Time Remaining</label></div>
                            <div class="form-check" t-if="'remaining_hours_so' in fc_fields"><input class="form-check-input" type="checkbox" data-field="remaining_hours_so" id="fc_remaining_hours_so"/><label class="form-check-label" for="fc_remaining_hours_so">Remaining on SO</label></div>
                            <div class="form-check" t-if="'progress' in fc_fields"><input class="form-check-input" type="checkbox" data-field="progress" id="fc_progress"/><label class="form-check-label" for="fc_progress">Progress</label></div>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="date_last_stage_update" id="fc_date_last_stage_update"/><label class="form-check-label" for="fc_date_last_stage_update">Last Stage Update</label></div>
                            <div class="form-check"><input class="form-check-input" type="checkbox" data-field="project_id" id="fc_project_id"/><label class="form-check-label" for="fc_project_id">Project</label></div>
                        </div>
                    </div>
                </div>
            </xpath>
            <xpath expr="//t[@t-call='project.portal_tasks_list']" position="after">
                <!-- Add Task Modal -->
                <div t-if="is_editor" class="modal fade" id="addTaskModal" tabindex="-1" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Add Task</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                            </div>
                            <div class="modal-body">
                                <div class="mb-3">
                                    <label for="taskName" class="form-label">Task Name</label>
                                    <input type="text" class="form-control" id="taskName" placeholder="e.g. Send Invitations" required="required"/>
                                </div>
                                <div class="mb-3" t-if="available_stages">
                                    <label for="taskStage" class="form-label">Stage</label>
                                    <select class="form-select" id="taskStage">
                                        <t t-foreach="available_stages" t-as="stage">
                                            <option t-att-value="stage.id"><t t-esc="stage.name"/></option>
                                        </t>
                                    </select>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="button" class="btn btn-primary" id="btnSubmitTask">Create Task</button>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Add Stage Modal -->
                <div t-if="is_editor" class="modal fade" id="addStageModal" tabindex="-1" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Add Stage</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                            </div>
                            <div class="modal-body">
                                <div class="mb-3">
                                    <label for="stageName" class="form-label">Stage Name</label>
                                    <input type="text" class="form-control" id="stageName" placeholder="e.g. In Progress" required="required"/>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="button" class="btn btn-primary" id="btnSubmitStage">Create Stage</button>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>
        </field>
    </record>

    <!-- Task Detail Editor (for full-edit collaborators) -->
    <record id="portal_task_editor" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Task Editor Controls</field>
        <field name="inherit_id" ref="project.portal_my_task"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <!-- Pencil button next to task name → opens Edit Task modal -->
            <xpath expr="//span[@t-field='task.name']" position="after">
                <button t-if="is_editor" class="btn btn-sm btn-link wt-edit-btn p-0 ms-2" data-bs-toggle="modal" data-bs-target="#editTaskModal" title="Edit task"><i class="fa fa-pencil"/></button>
            </xpath>

            <!-- Pencil button next to stage badge → opens Edit Task modal -->
            <xpath expr="//span[@t-field='task.stage_id.name']" position="after">
                <button t-if="is_editor" class="btn btn-sm btn-link wt-edit-btn p-0 ms-1" data-bs-toggle="modal" data-bs-target="#editTaskModal" title="Edit task"><i class="fa fa-pencil"/></button>
            </xpath>

            <!-- Pencil button next to deadline → opens Edit Task modal -->
            <xpath expr="//span[@t-field='task.date_deadline']" position="after">
                <button t-if="is_editor" class="btn btn-sm btn-link wt-edit-btn p-0 ms-1" data-bs-toggle="modal" data-bs-target="#editTaskModal" title="Edit task"><i class="fa fa-pencil"/></button>
            </xpath>

            <!-- Add deadline label when task has no deadline → opens Edit Task modal -->
            <xpath expr="//div[@name='portal_my_task_allocated_hours']" position="before">
                <div t-if="is_editor and not task.date_deadline">
                    <strong>Deadline:</strong>
                    <button class="btn btn-sm btn-link wt-edit-btn p-0 ms-1" data-bs-toggle="modal" data-bs-target="#editTaskModal" title="Set deadline"><i class="fa fa-plus"/></button>
                </div>
            </xpath>

            <!-- Pencil button next to Description title → opens Edit Task modal -->
            <xpath expr="//div[@t-field='task.description']" position="after">
                <button t-if="is_editor" class="btn btn-sm btn-link wt-edit-btn p-0 ms-2 wt-desc-edit-btn" data-bs-toggle="modal" data-bs-target="#editTaskModal" title="Edit task"><i class="fa fa-pencil"/></button>
            </xpath>

            <!-- Add description button when empty → opens Edit Task modal -->
            <xpath expr="//div[@id='card_body']" position="inside">
                <div t-if="is_editor and is_html_empty(task.description)" class="mt-3 mb-3">
                    <button class="btn btn-sm btn-link wt-edit-btn p-0" data-bs-toggle="modal" data-bs-target="#editTaskModal" title="Add description"><i class="fa fa-plus me-1"/>Add Description</button>
                </div>
            </xpath>

            <!-- Edit Task Modal -->
            <xpath expr="//div[@id='card']" position="inside">
                <div t-if="is_editor" class="modal fade" id="editTaskModal" tabindex="-1" aria-hidden="true">
                    <div class="modal-dialog modal-dialog-centered modal-lg">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Edit Task</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                            </div>
                            <div class="modal-body">
                                <div class="mb-3">
                                    <label class="form-label fw-bold" for="editTaskName">Name</label>
                                    <input type="text" class="form-control" id="editTaskName" t-att-value="task.name"/>
                                </div>
                                <div class="row g-3 mb-3">
                                    <div class="col-12 col-sm-6" t-if="available_stages">
                                        <label class="form-label fw-bold" for="editTaskStage">Stage</label>
                                        <select class="form-select" id="editTaskStage">
                                            <t t-foreach="available_stages" t-as="s">
                                                <option t-att-value="s.id" t-att-selected="s.id == task.stage_id.id"><t t-esc="s.name"/></option>
                                            </t>
                                        </select>
                                    </div>
                                    <div t-att-class="'col-12 col-sm-6' if available_stages else 'col-12'">
                                        <label class="form-label fw-bold" for="editTaskDeadline">Deadline</label>
                                        <input type="datetime-local" class="form-control" id="editTaskDeadline"
                                               t-att-value="task.date_deadline.strftime('%Y-%m-%dT%H:%M') if task.date_deadline else ''"/>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label fw-bold" for="editTaskDescription">Description</label>
                                    <textarea class="form-control" id="editTaskDescription" rows="6"
                                              placeholder="Add details about this task..."><t t-esc="task.description and task.description.striptags() or ''"/></textarea>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="button" class="btn btn-primary" id="btnSaveTask">Save Changes</button>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>

            <!-- Sub-tasks section (for editors) — inside #card -->
            <xpath expr="//div[@id='card']" position="inside">
                <div t-if="is_editor" class="wt-subtasks-section mt-4" id="task_subtasks" data-anchor="true">
                    <h5 class="mb-1">Sub-tasks</h5>
                    <hr class="mt-1 mb-2"/>
                    <table t-if="task.child_ids" class="table table-sm wt-subtask-table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Stage</th>
                                <th>Deadline</th>
                                <th style="width:40px;"/>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="task.sudo().child_ids" t-as="subtask">
                                <tr>
                                    <td>
                                        <a t-attf-href="/my/projects/#{project.id}/task/#{subtask.id}">
                                            #<t t-esc="subtask.id"/> <t t-esc="subtask.name"/>
                                        </a>
                                    </td>
                                    <td><span class="badge rounded-pill text-bg-info"><t t-esc="subtask.stage_id.name"/></span></td>
                                    <td t-if="subtask.date_deadline"><span t-field="subtask.date_deadline" t-options='{"widget": "date"}'/></td>
                                    <td t-else=""/>
                                    <td>
                                        <button class="btn btn-sm btn-link text-danger p-0 wt-delete-subtask"
                                                t-att-data-subtask-id="subtask.id" title="Delete sub-task">
                                            <i class="fa fa-trash"/>
                                        </button>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    <p t-if="not task.child_ids" class="text-muted small">No sub-tasks yet.</p>
                    <button class="btn btn-sm btn-outline-primary mt-2" data-bs-toggle="modal" data-bs-target="#addSubtaskModal">
                        <i class="fa fa-plus me-1"/>Add Sub-task
                    </button>
                    <!-- Add Sub-task Modal -->
                    <div class="modal fade" id="addSubtaskModal" tabindex="-1" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title">Add Sub-task</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                                </div>
                                <div class="modal-body">
                                    <div class="mb-3">
                                        <label class="form-label fw-bold" for="subtaskName">Name</label>
                                        <input type="text" class="form-control" id="subtaskName" placeholder="Sub-task name..." required="required"/>
                                    </div>
                                    <div class="row g-3">
                                        <div class="col-12 col-sm-6" t-if="available_stages">
                                            <label class="form-label fw-bold" for="subtaskStage">Stage</label>
                                            <select class="form-select" id="subtaskStage">
                                                <t t-foreach="available_stages" t-as="stage">
                                                    <option t-att-value="stage.id"><t t-esc="stage.name"/></option>
                                                </t>
                                            </select>
                                        </div>
                                        <div t-att-class="'col-12 col-sm-6' if available_stages else 'col-12'">
                                            <label class="form-label fw-bold" for="subtaskDeadline">Deadline</label>
                                            <input type="datetime-local" class="form-control" id="subtaskDeadline"/>
                                        </div>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                    <button type="button" class="btn btn-primary" id="btnAddSubtask">Create Sub-task</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>
        </field>
    </record>

    <!-- Opportunities Card View -->
    <record id="portal_opportunities_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Opportunities Card View</field>
        <field name="inherit_id" ref="website_crm_partner_assign.portal_my_opportunities"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid-2" t-if="opportunities">
                    <t t-foreach="opportunities" t-as="opp">
                        <a t-attf-href="/my/opportunity/#{opp.id}" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-line-chart" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="opp.name"/></span>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Contact</span>
                                    <span class="wt-pc-value"><t t-esc="opp.contact_name or ''"/></span>
                                </div>
                                <div class="wt-pc-row" t-if="opp.expected_revenue">
                                    <span class="wt-pc-label">Revenue</span>
                                    <span class="wt-pc-value wt-pc-total"><t t-esc="opp.expected_revenue" t-options='{"widget": "monetary", "display_currency": opp.company_currency}'/></span>
                                </div>
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Stage</span>
                                    <span class="wt-pc-value"><t t-esc="opp.stage_id.name"/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View opportunity</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not opportunities"><p class="alert alert-warning">There are currently no opportunities.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Manufacturing Orders Card View -->
    <record id="portal_productions_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Manufacturing Card View</field>
        <field name="inherit_id" ref="mrp_subcontracting.portal_my_productions"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid" t-if="pickings">
                    <t t-foreach="pickings" t-as="picking">
                        <a t-attf-href="/my/productions/#{picking.id}" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-industry" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="picking.name"/></span>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row" t-if="picking.origin">
                                    <span class="wt-pc-label">Source</span>
                                    <span class="wt-pc-value"><t t-esc="picking.origin"/></span>
                                </div>
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Scheduled</span>
                                    <span class="wt-pc-value"><t t-esc="picking.scheduled_date" t-options='{"widget": "date"}'/></span>
                                </div>
                                <div class="wt-pc-row" t-if="picking.date_deadline">
                                    <span class="wt-pc-label">Deadline</span>
                                    <span class="wt-pc-value"><t t-esc="picking.date_deadline" t-options='{"widget": "date"}'/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View order</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not pickings"><p class="alert alert-warning">There are currently no manufacturing orders.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- RFQ Card View -->
    <record id="portal_rfq_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow RFQ Card View</field>
        <field name="inherit_id" ref="purchase.portal_my_purchase_rfqs"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid" t-if="rfqs">
                    <t t-foreach="rfqs" t-as="rfq">
                        <a t-att-href="rfq.get_portal_url()" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-clipboard" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="rfq.name"/></span>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Order Date</span>
                                    <span class="wt-pc-value"><t t-esc="rfq.date_order" t-options='{"widget": "date"}'/></span>
                                </div>
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Total</span>
                                    <span class="wt-pc-value wt-pc-total"><t t-esc="rfq.amount_total" t-options='{"widget": "monetary", "display_currency": rfq.currency_id}'/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View RFQ</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not rfqs"><p class="alert alert-warning">There are currently no requests for quotation.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Purchase Orders Card View -->
    <record id="portal_purchase_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Purchase Orders Card View</field>
        <field name="inherit_id" ref="purchase.portal_my_purchase_orders"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid" t-if="orders">
                    <t t-foreach="orders" t-as="order">
                        <a t-att-href="order.get_portal_url()" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-truck" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="order.name"/></span>
                                <t t-if="order.state == 'cancel'"><span class="wt-badge wt-badge-cancelled">Cancelled</span></t>
                                <t t-elif="order.state == 'done'"><span class="wt-badge wt-badge-paid">Done</span></t>
                                <t t-elif="order.invoice_status == 'to invoice'"><span class="wt-badge wt-badge-waiting">To Invoice</span></t>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row" t-if="order.date_approve">
                                    <span class="wt-pc-label">Confirmed</span>
                                    <span class="wt-pc-value"><t t-esc="order.date_approve" t-options='{"widget": "date"}'/></span>
                                </div>
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Total</span>
                                    <span class="wt-pc-value wt-pc-total"><t t-esc="order.amount_total" t-options='{"widget": "monetary", "display_currency": order.currency_id}'/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View order</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not orders"><p class="alert alert-warning">There are currently no purchase orders.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Leads Card View -->
    <record id="portal_leads_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Leads Card View</field>
        <field name="inherit_id" ref="website_crm_partner_assign.portal_my_leads"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid" t-if="leads">
                    <t t-foreach="leads" t-as="lead">
                        <a t-attf-href="/my/lead/#{lead.id}" class="wt-portal-card">
                            <div class="wt-pc-head">
                                <div class="wt-pc-icon"><i class="fa fa-bullseye" style="font-size:14px;"/></div>
                                <span class="wt-pc-num"><t t-esc="lead.name"/></span>
                            </div>
                            <div class="wt-pc-meta">
                                <div class="wt-pc-row">
                                    <span class="wt-pc-label">Date</span>
                                    <span class="wt-pc-value"><t t-esc="lead.create_date" t-options='{"widget": "date"}'/></span>
                                </div>
                                <div class="wt-pc-row" t-if="lead.contact_name">
                                    <span class="wt-pc-label">Contact</span>
                                    <span class="wt-pc-value"><t t-esc="lead.contact_name"/></span>
                                </div>
                                <div class="wt-pc-row" t-if="lead.email_from">
                                    <span class="wt-pc-label">Email</span>
                                    <span class="wt-pc-value"><t t-esc="lead.email_from"/></span>
                                </div>
                                <div class="wt-pc-row" t-if="lead.phone">
                                    <span class="wt-pc-label">Phone</span>
                                    <span class="wt-pc-value"><t t-esc="lead.phone"/></span>
                                </div>
                            </div>
                            <div class="wt-pc-foot"><span>View lead</span><i class="fa fa-angle-right"/></div>
                        </a>
                    </t>
                </div>
                <t t-if="not leads"><p class="alert alert-warning">There are no leads.</p></t>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

    <!-- Timesheets Card View -->
    <record id="portal_timesheets_cards" model="ir.ui.view" forcecreate="False">
        <field name="name">Woow Timesheets Card View</field>
        <field name="inherit_id" ref="hr_timesheet.portal_my_timesheets"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='portal.portal_table']" position="replace">
                <div class="wt-card-grid">
                    <t t-foreach="grouped_timesheets" t-as="timesheets_with_hours">
                        <t t-set="timesheets" t-value="timesheets_with_hours[0]"/>
                        <t t-foreach="timesheets" t-as="timesheet">
                            <div class="wt-portal-card" style="cursor: default;">
                                <div class="wt-pc-head">
                                    <div class="wt-pc-icon"><i class="fa fa-clock-o" style="font-size:14px;"/></div>
                                    <span class="wt-pc-num"><t t-esc="timesheet.name or 'Timesheet'"/></span>
                                </div>
                                <div class="wt-pc-meta">
                                    <div class="wt-pc-row">
                                        <span class="wt-pc-label">Date</span>
                                        <span class="wt-pc-value"><t t-esc="timesheet.date" t-options='{"widget": "date"}'/></span>
                                    </div>
                                    <div class="wt-pc-row">
                                        <span class="wt-pc-label">Employee</span>
                                        <span class="wt-pc-value"><t t-esc="timesheet.employee_id.name"/></span>
                                    </div>
                                    <div class="wt-pc-row">
                                        <span class="wt-pc-label">Project</span>
                                        <span class="wt-pc-value"><t t-esc="timesheet.project_id.name"/></span>
                                    </div>
                                    <div class="wt-pc-row" t-if="timesheet.task_id">
                                        <span class="wt-pc-label">Task</span>
                                        <span class="wt-pc-value"><t t-esc="timesheet.task_id.name"/></span>
                                    </div>
                                    <div class="wt-pc-row">
                                        <span class="wt-pc-label">Time Spent</span>
                                        <span class="wt-pc-value wt-pc-total"><t t-esc="timesheet.unit_amount" t-options='{"widget": "float_time"}'/></span>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </t>
                </div>
                <div t-if="pager" class="o_portal_pager d-flex justify-content-center my-3"><t t-call="portal.pager"/></div>
            </xpath>
        </field>
    </record>

</odoo>
